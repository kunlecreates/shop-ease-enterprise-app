name: CI — user-service

on:
  push:
    paths:
      - 'services/user-service/**'
  pull_request:
    paths:
      - 'services/user-service/**'

jobs:
  test:
    name: User Service Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Build & install test-utils (for shared test migrations)
        run: |
          if [ -f ./services/test-utils/pom.xml ]; then
            mvn -B -f ./services/test-utils/pom.xml install
          fi

      - name: Cache Maven
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: maven-${{ runner.os }}-user-${{ hashFiles('services/user-service/**/pom.xml') }}
          restore-keys: |
            maven-${{ runner.os }}-user-

      - name: Run Maven tests
        working-directory: services/user-service
        run: |
          mvn -B -Dapi.version=1.44 test
