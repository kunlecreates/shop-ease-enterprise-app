name: Playwright Smoke Tests (test)

on:
  push:
    branches:
      - develop

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Echo
        run: echo "Playwright smoke workflow minimal test"
