This folder contains CI artifacts copied from a workflow run for local inspection.

Structure:
- `run19618128336/playwright-debug-artifacts/logs/` : runtime logs (pg.log, shop-api.log, etc.)

Use `scripts/copy-ci-artifacts.sh` to import artifacts into the workspace for inspection.
